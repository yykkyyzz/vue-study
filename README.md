# vue-study

```
$ git clone https://github.com/yykkyyzz/vue-study.git

$ git fetch --all

$ git pull origin main
```

## current directory

```shell
$ npm run dev
```

## current directory

npm server で port 3000 を使用するので、json-server では 3001 を使用する

```shell
$ json-server db.json -p 3001
```